pre-commit:
  commands:
    lint:
      glob: '*.go'
      run: golangci-lint run -v
    test:
      glob: '*.go'
      run: go test ./...

pre-push:
  commands:
    lint:
      glob: '*.go'
      run: golangci-lint run -v
    test:
      glob: '*.go'
      run: go test ./...
